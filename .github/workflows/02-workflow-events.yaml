name : 02 Workflow Events
on : workflow_dispatch
jobs : 
    echo:
        runs-on : ubuntu-latest
        steps : 
            -   name : Show the Triggers
                run : echo "this is aa(n) ${{ github.event_name }} event." 